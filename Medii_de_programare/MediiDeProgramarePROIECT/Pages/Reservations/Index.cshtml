@page
@model MediiDeProgramarePROIECT.Pages.Reservations.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
               Email Client
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Reservation[0].TableID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Reservation[0].ReservationDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Reservation[0].ReservationTime)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Reservation[0].ReservationDuration)
            </th>
            <th>Acțiuni</th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Reservation) {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Client.Email)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.TableID)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.ReservationDate)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.ReservationTime)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.ReservationDuration)
                </td>

            <td>
          
                <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
